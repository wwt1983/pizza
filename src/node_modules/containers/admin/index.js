import React from 'react'

import {getPizzesFromDb} from './../../actions'

const Admin  =() => (
    <div>
        <div className="container">
admin
            <button className="btn btn-danger btn-xs" onClick={() => getPizzesFromDb()}>Заполнить базу</button>

        </div>
    </div>
)


export default Admin
