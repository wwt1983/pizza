import React, {Component} from 'react'
import {Link} from 'react-router'
import R from 'ramda'
import {connect} from 'react-redux'

import {fetchPizza, addPizzaToBasket} from 'actions'
import {getPizza} from 'selectors'

class Pizza extends Component {
    componentDidMount(){
    this.props.fetchPizza()
    }

    renderPhone(pizza, index) {
        const {addPizzaToBasket} = this.props;
        const shortDeacription = `${R.take(90, pizza.description)}...`
        return (
            <div className='col-sm-4 col-lg-4 col-md-4 book-list' key={index}>
                <div className='thumbnail'>
                    <img
                        className='img-thumbnail'
                        src={pizza.image}
                        alt={pizza.name}
                    />
                    <div
                        className='caption'>
                        <h4 className='pull-right'>{pizza.price} руб.</h4>
                        <h4>
                            <Link
                                to={`/pizza/${pizza.id}`}>
                                {pizza.name}
                            </Link>
                        </h4>
                        <p>{shortDeacription}</p>
                        <p className='itemButton'>
                            <button
                                className='btn btn-primary'
                            onClick={() => addPizzaToBasket(pizza.id)}
                            >
                                В корзину
                            </button>
                     {/*       <Link
                                to={`/pizza/${pizza.id}`}
                                className='btn btn-default'>
                                Подробнее
                            </Link>*/}
                        </p>
                    </div>
                </div>
            </div>
        )
    }
    render() {
        const {pizza} = this.props;
        return(
            <div className='books row'>
                {pizza.map((p, index) => this.renderPhone(p, index))}
            </div>
        )
    }
}

const mapStateToProps = state => ({
    pizza : getPizza(state)
})

const mapDispatchToProps = {
    fetchPizza,
    addPizzaToBasket
}
export default connect(mapStateToProps, mapDispatchToProps)(Pizza)

